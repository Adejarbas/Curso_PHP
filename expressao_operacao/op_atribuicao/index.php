<?php

    $a = 0;
    $a += 10; // Atribuição de adição $a = $a + 10;

    echo "Atribuição de adição: $a <br>";


    $b = 0;
    $b -= 5; // Atribuição de subtração $b = $b - 5;

    echo "Atribuição de subtração: $b<br>";

    $c = 5;
    $c *= 2; // Atribuição de multiplicação $c = $c * 2;

    echo "Atribuição de multiplicação: $c <br>";

    $d = 5;
    $d /= 2; // Atribuição de divi $a = $a / 2

    echo "Atribuição de divisão: $d <br>";

    $e = 5;
    $e %= 2; // Atribuição de resto $e = $e % 2;

    echo "Atribuição de resto: $e <br>";


    // utilizando variaveis

    $f = 10;
    $x = 20;

    $f += $x; // Atribuição de adição $f = $f + $x;

    echo "Atribuição de adição com variaveis: $f <br>";