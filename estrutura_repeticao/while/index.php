<?php

    // Definição do contador
    $x = 0;

    // Inicio / Definição do loop while
    while ( $x < 10 ) {

        // Corpo do loop
        echo $x . "<br>";

        // Incremento do contador
        $x = $x + 1;
    }

    echo "Fim do loop while!<br>";


        // Definição do contador
    $y = 0;

    // Inicio / Definição do loop while
    while ( $y <= 10 ) {

        // Corpo do loop
        echo $y . "<br>";

        // Incremento do contador
        $y = $y + 2;
    }
////////////////
        // Definição do contador
    $z = 10;

    // Inicio / Definição do loop while
    while ( $z > 0 ) {

        // Corpo do loop
        echo $z . "<br>";

        // Incremento do contador
        $z --;
    }
/////
        // Definição do contador
    $a = 10;

    // Inicio / Definição do loop while
    while ( $a > 0 ) {

        // Corpo do loop
        if($a % 2 != 0) {
            echo $a . "<br>";
        }

        // Incremento do contador
        $a --;
    }